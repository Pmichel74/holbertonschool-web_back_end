// Test file for ES6 code
const countStudents = require('./2-es6');

// Sample data
const studentList = ['Alice', 'Bob', 'Charlie', 'David'];

// Test the function
const report = countStudents(studentList);
console.log('Report object:', report);
